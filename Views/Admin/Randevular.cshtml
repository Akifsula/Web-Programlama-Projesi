@model IEnumerable<KuaforYonetim.Models.Randevu>

<h2>Bekleyen Randevular</h2>

<table class="table">
    <thead>
        <tr>
            <th>Tarih</th>
            <th>Çalışan</th>
            <th>Hizmet</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var randevu in Model)
        {
            <tr>
                <td>@randevu.Tarih.ToString("g")</td>
                <td>@randevu.Calisan.AdSoyad</td>
                <td>@randevu.Hizmet.Ad</td>
                <td>
                    <a class="btn btn-success btn-sm" asp-action="Onayla" asp-route-id="@randevu.RandevuId">Onayla</a>
                    <a class="btn btn-danger btn-sm" asp-action="Reddet" asp-route-id="@randevu.RandevuId">Reddet</a>
                </td>
            </tr>
        }
    </tbody>
</table>